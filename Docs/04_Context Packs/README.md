# Context Packs

Timestamped AI/developer context ZIP files belong here.

A context pack should contain the canonical documentation plus only the source files relevant to the next task.

Do not include `node_modules`, Android build caches, the entire Archive or unrelated generated output.
